export const SITE_CONFIG = Object.freeze({
  doi: "10.5281/zenodo.0000000",
  doiUrl: "",
  maxLocalPhotos: 100,
  maxUploadBytes: 30 * 1024 * 1024,
  maxStoredDimension: 1800,
  maxRenderDimension: 1200,
  storedImageQuality: 0.84
});
